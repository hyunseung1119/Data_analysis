<div align="center">

# 🤖 Multi-Agent Decision System

**AI 기반 데이터 분석 및 의사결정 지원 플랫폼**

[![Python](https://img.shields.io/badge/Python-3.10+-3776AB?logo=python&logoColor=white)](https://python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-009688?logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com)
[![React](https://img.shields.io/badge/React-18+-61DAFB?logo=react&logoColor=black)](https://react.dev)
[![LangGraph](https://img.shields.io/badge/LangChain-LangGraph-1C3C3C?logo=langchain)](https://langchain.com)

[핵심 기능](#-핵심-기능) •
[시스템 아키텍처](#-시스템-아키텍처) •
[기술 스택](#-기술-스택) •
[핵심 구현](#-핵심-구현) •
[시작하기](#-시작하기)

</div>

---

## 🎯 프로젝트 개요

> **"데이터 분석의 민주화"**  
> 누구나 쉽게 전문적인 데이터 분석을 수행할 수 있는 AI 플랫폼

**Multi-Agent Decision System**은 다중 AI 에이전트가 협력하여 복잡한 데이터 분석과 의사결정을 지원하는 **풀스택 AI 플랫폼**입니다.

### 💡 해결하고자 한 문제
- 데이터 분석에 필요한 **코딩 장벽 해소**
- **반복적인 EDA 작업 자동화**
- **분석 → 인사이트 → 액션** 사이클 단축
- **비개발자도 활용 가능한** 데이터 분석 도구 제공

### 📈 프로젝트 성과
| 지표 | 결과 |
|:---|:---|
| 분석 자동화 | CSV 업로드 → 10초 내 EDA 완료 |
| 지원 분석 유형 | 8개 (EDA, 시계열, 세그먼트, 상관관계, A/B 테스트, 예측, AI 인사이트, 비즈니스 메트릭) |
| AI 기능 | 자연어 코드 생성, 인사이트 자동 시각화, 컬럼 자동 설명 |
| 에이전트 수 | 5개 전문 분야별 AI 에이전트 |

---

## ✨ 핵심 기능

### 📊 자동 데이터 분석 파이프라인

| 기능 | 설명 | 핵심 기술 |
|:---|:---|:---|
| **AI 컬럼 분석** | 컬럼 의미 및 분석 팁 자동 설명 | GPT-4 Structured Output |
| **EDA 대시보드** | 분포, 결측치, 중복, 기술통계 분석 | Pandas, NumPy |
| **시계열 분석** | 트렌드, 계절성, 미래 예측 | Linear Regression, Statsmodels |
| **세그먼트 분석** | 그룹 비교, ANOVA 검정 | SciPy Stats |
| **상관관계 분석** | 히트맵, 피어슨 상관계수 | Pandas Correlation |
| **A/B 테스트** | 유의성 검정, 효과 크기(Cohen's d) | T-test, Statistical Power |
| **예측 분석** | 미래 값 예측 + 95% 신뢰구간 | Regression Analysis |
| **비즈니스 메트릭** | KPI 자동 계산 및 시각화 | Custom Analytics Engine |

---

### 🤖 AI 자동화 기능

| 기능 | 사용자 경험 | 기술 |
|:---|:---|:---|
| **AI 인사이트 리포트** | 주요 패턴 자동 발견, 리스크/기회 식별 | GPT-4 + Statistical Analysis |
| **전처리 진단** | 데이터 품질 이슈 자동 탐지 및 해결 제안 | Rule-based + LLM |
| **자연어 → Pandas 코드** | "결측치를 평균으로 채워줘" → 코드 생성 | Few-shot Prompting |
| **보안 코드 실행** | 생성된 코드 안전하게 실행 | Sandboxed Execution |
| **경영진 요약** | 비전문가용 인사이트 리포트 자동 생성 | Summarization |

---

### 🔮 예측 및 이상 탐지

| 기능 | 설명 |
|:---|:---|
| **시계열 예측** | 미래 값 + 95% 신뢰구간 시각화 |
| **What-if 분석** | 변수 변화 영향 시뮬레이션 |
| **이상 탐지** | IQR / Z-score 기반 이상치 자동 탐지 |

---

## 🏗 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           Frontend (React 18)                           │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐│
│  │ Dashboard   │ │ Data        │ │ Chat        │ │ Analysis Dashboard  ││
│  │ Page        │ │ Analysis    │ │ Page        │ │ (EDA, Forecast...)  ││
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        Backend (FastAPI + LangGraph)                    │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │                      Multi-Agent Workflow                           ││
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐            ││
│  │  │ Analyze  │→ │ Execute  │→ │ Execute  │→ │Synthesize│            ││
│  │  │  Node    │  │ Agent 1  │  │ Agent N  │  │  Node    │            ││
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘            ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                    Specialist Agents                              │  │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────────┐   │  │
│  │  │   Data     │ │ Calculator │ │    Risk    │ │  Strategist  │   │  │
│  │  │  Analyst   │ │            │ │  Analyst   │ │              │   │  │
│  │  └────────────┘ └────────────┘ └────────────┘ └──────────────┘   │  │
│  │  ┌────────────┐                                                   │  │
│  │  │ Law Expert │                                                   │  │
│  │  └────────────┘                                                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                      Analysis Routes (13개)                       │  │
│  │  • AI Insights    • Code Generator  • Forecast    • A/B Test     │  │
│  │  • Time Series    • Segment         • Correlation • Preprocess   │  │
│  │  • Column Explain • Business Metrics • Charts     • Core EDA     │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          External Services                              │
│           ┌─────────────────────────────────────────┐                   │
│           │          OpenAI GPT-4.1-mini            │                   │
│           │          (LangChain Integration)        │                   │
│           └─────────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🛠 기술 스택

### Backend
| 기술 | 용도 | 선택 이유 |
|:---|:---|:---|
| **FastAPI** | 비동기 REST API | 자동 OpenAPI 문서화, 고성능 async 지원 |
| **LangGraph** | Multi-Agent 오케스트레이션 | 상태 기반 워크플로우, 에이전트 간 협업 |
| **LangChain** | LLM 통합 | 체계적인 프롬프트 관리, 다양한 LLM 지원 |
| **Pandas / NumPy** | 데이터 분석 | 업계 표준 데이터 처리 라이브러리 |
| **SciPy** | 통계 분석 | ANOVA, T-test 등 고급 통계 검정 |

### Frontend
| 기술 | 용도 | 선택 이유 |
|:---|:---|:---|
| **React 18** | UI 프레임워크 | 컴포넌트 기반 개발, 풍부한 생태계 |
| **Vite** | 빌드 도구 | 빠른 HMR, ESM 기반 최적화 |
| **Recharts** | 데이터 시각화 | React 친화적, 선언적 차트 API |
| **TailwindCSS** | 스타일링 | 유틸리티 우선, 빠른 프로토타이핑 |
| **Zustand** | 상태 관리 | 가벼운 전역 상태 관리 |

### AI / ML
| 기술 | 용도 | 선택 이유 |
|:---|:---|:---|
| **GPT-4.1-mini** | 텍스트 생성, 코드 생성 | 비용 효율적, 빠른 응답 |
| **Structured Output** | JSON 기반 출력 | 안정적인 파싱, 타입 안전성 |
| **Few-shot Prompting** | 코드 생성 품질 향상 | 맥락 이해도 향상 |

---

## 🔬 핵심 구현

### 1. Multi-Agent Workflow (LangGraph)

```python
# LangGraph 기반 상태 머신 워크플로우
class MultiAgentWorkflow:
    def _build_graph(self):
        graph = StateGraph(MultiAgentState)
        
        # 노드 추가
        graph.add_node("analyze", self._analyze_node)      # 요청 분석
        graph.add_node("execute", self._execute_agent_node) # 에이전트 실행
        graph.add_node("synthesize", self._synthesize_node) # 결과 종합
        
        # 엣지 정의 (조건부 라우팅)
        graph.add_edge("analyze", "execute")
        graph.add_conditional_edges("execute", self._should_continue)
        graph.add_edge("synthesize", END)
```

**핵심 설계:**
- **상태 기반**: `MultiAgentState`로 전체 실행 상태 추적
- **동적 라우팅**: 요청 분석 결과에 따라 필요한 에이전트만 실행
- **결과 종합**: 여러 에이전트 결과를 하나의 리포트로 통합

---

### 2. Orchestrator 패턴

```python
class Orchestrator:
    def analyze_request(self, query: str) -> Dict:
        """LLM을 활용해 실행할 에이전트 결정"""
        # 자연어 → 에이전트 매핑
        
    def execute_agents(self, query: str, agent_names: List[str]):
        """선택된 에이전트 순차 실행"""
        
    def synthesize_results(self, query: str, results: List):
        """결과 종합 → 최종 리포트 생성"""
```

**에이전트 협업 흐름:**
```
사용자 질문 → 요청 분석 → 에이전트 선택 → 순차 실행 → 결과 종합 → 최종 리포트
```

---

### 3. 자연어 → Pandas 코드 생성

```python
@router.post("/analysis/ai-preprocess/generate-code")
async def generate_preprocessing_code(request: CodeGenerationRequest):
    # 데이터프레임 메타정보 수집
    df_info = {
        "columns": list(df.columns),
        "dtypes": {col: str(df[col].dtype) for col in df.columns},
        "sample": df.head(3).to_dict(orient="records"),
    }
    
    # LLM에 코드 생성 요청
    response = await llm.chat(system_prompt, user_message)
    return {"code": result["code"], "explanation": result["explanation"]}
```

**안전한 코드 실행:**
```python
# 위험 키워드 필터링
dangerous_keywords = ['os.', 'subprocess', 'exec(', 'eval(', '__', 'open(', 'import ']

# 샌드박스 실행
local_vars = {"df": df.copy()}
exec(code, {"__builtins__": {}}, local_vars)  # builtins 비활성화
```

---

### 4. 전문 에이전트 구조

```python
class DataAnalystAgent(BaseAgent):
    """데이터 분석 전문 에이전트"""
    
    def execute(self, input: AgentInput) -> AgentOutput:
        # CSV 프로파일링
        profile = self._profile_data(df)
        
        # 상관관계 분석
        correlation = self._analyze_correlation(df)
        
        # 시각화 데이터 생성
        visualizations = self._create_distribution_viz(df)
        
        return AgentOutput(
            result=report,
            visualizations=visualizations,
            confidence=0.85
        )
```

**5개 전문 에이전트:**
| 에이전트 | 역할 |
|:---|:---|
| `DataAnalyst` | CSV 프로파일링, 상관관계, A/B 테스트 |
| `Calculator` | 수치 계산, 재무 분석 |
| `RiskAnalyst` | 리스크 평가, 위험 요소 식별 |
| `Strategist` | 전략 제안, 의사결정 지원 |
| `LawExpert` | 법률/규정 관련 분석 |

---

## 📁 프로젝트 구조

```
multi_agent_decision/
├── backend/
│   ├── agents/                    # 에이전트 시스템
│   │   ├── base.py               # BaseAgent 추상 클래스
│   │   ├── orchestrator.py       # 에이전트 오케스트레이터
│   │   ├── registry.py           # 에이전트 레지스트리
│   │   └── specialists/          # 전문 에이전트들
│   │       ├── data_analyst.py   # 데이터 분석 에이전트
│   │       ├── calculator.py     # 계산 에이전트
│   │       ├── risk_analyst.py   # 리스크 분석 에이전트
│   │       ├── strategist.py     # 전략 에이전트
│   │       └── law_expert.py     # 법률 전문 에이전트
│   ├── api/
│   │   ├── main.py               # FastAPI 앱 진입점
│   │   ├── routes/
│   │   │   ├── chat.py           # 멀티에이전트 채팅 API
│   │   │   └── analysis/         # 분석 API 라우트 (13개)
│   │   │       ├── ai_insights.py
│   │   │       ├── code_generator.py
│   │   │       ├── timeseries.py
│   │   │       └── ...
│   │   └── middleware/           # CORS 등 미들웨어
│   ├── core/
│   │   ├── workflow.py           # LangGraph 워크플로우
│   │   └── state.py              # 상태 정의
│   └── services/
│       └── llm_service.py        # OpenAI LLM 래퍼
├── frontend/
│   └── src/
│       ├── pages/                # 페이지 컴포넌트
│       │   ├── DashboardPage.jsx
│       │   ├── DataAnalysisPage.jsx
│       │   ├── ChatPage.jsx
│       │   └── AnalysisDashboardPage.jsx
│       ├── components/
│       │   ├── analysis/         # 분석 탭 컴포넌트 (19개)
│       │   │   ├── EDATab.jsx
│       │   │   ├── TimeSeriesTab.jsx
│       │   │   ├── ForecastTab.jsx
│       │   │   ├── AIInsightsTab.jsx
│       │   │   └── ...
│       │   └── charts/           # 차트 컴포넌트
│       ├── services/             # API 서비스
│       └── stores/               # Zustand 스토어
└── requirements.txt
```

---

## 🚀 시작하기

### 사전 요구사항
- Python 3.10+
- Node.js 18+
- OpenAI API Key

### Backend 설정

```bash
# 저장소 클론
git clone https://github.com/yourusername/multi-agent-decision.git
cd multi-agent-decision

# 가상환경 생성 및 활성화
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
cd backend
pip install -r requirements.txt

# 환경변수 설정
cp .env.example .env
# .env 파일에 OPENAI_API_KEY 설정

# 서버 실행
uvicorn api.main:app --reload --port 8001
```

### Frontend 설정

```bash
cd frontend
npm install
npm run dev
```

서버 실행 후:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8001
- **API 문서**: http://localhost:8001/docs

---

## 📊 적용된 이론 및 기법

| 분야 | 이론/기법 | 적용 위치 |
|:---|:---|:---|
| **통계학** | T-test, ANOVA, Pearson 상관계수 | A/B 테스트, 세그먼트 분석 |
| **AI 패턴** | ReAct (Reasoning + Acting) | 에이전트 의사결정 |
| **소프트웨어 설계** | Orchestrator 패턴, Registry 패턴 | 에이전트 관리 |
| **보안** | Sandboxed Execution | 코드 실행 |
| **데이터 과학** | IQR, Z-score | 이상치 탐지 |

---

## 📝 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다.

---

<div align="center">

**Made with ❤️ for Data Democratization**

</div>
