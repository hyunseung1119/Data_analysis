<div align="center">

# 🤖 Multi-Agent Decision System

**AI 기반 데이터 분석 및 의사결정 지원 플랫폼**

[![Python](https://img.shields.io/badge/Python-3.10+-3776AB?logo=python&logoColor=white)](https://python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-009688?logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com)
[![React](https://img.shields.io/badge/React-18+-61DAFB?logo=react&logoColor=black)](https://react.dev)
[![LangGraph](https://img.shields.io/badge/LangChain-LangGraph-1C3C3C?logo=langchain)](https://langchain.com)

[핵심 기능](#-핵심-기능) • [시스템 아키텍처](#-시스템-아키텍처) • [기술 스택](#-기술-스택) • [핵심 구현](#-핵심-구현) • [적용 이론](#-적용된-이론-및-기법)

</div>

---

## 🎯 프로젝트 개요

> **"데이터 분석의 민주화"** - 누구나 쉽게 전문적인 데이터 분석을 수행할 수 있는 AI 플랫폼

**Multi-Agent Decision System**은 다중 AI 에이전트가 협력하여 복잡한 데이터 분석과 의사결정을 지원하는 **풀스택 AI 플랫폼**입니다.

### 💡 해결하고자 한 문제
- 데이터 분석에 필요한 **코딩 장벽** 해소
- **반복적인 EDA 작업** 자동화
- **분석 → 인사이트 → 액션** 사이클 단축
- **비개발자도 활용 가능한** 데이터 분석 도구 제공

### � 프로젝트 성과
| 지표 | 결과 |
|:---|:---|
| 분석 자동화 | CSV 업로드 → 10초 내 EDA 완료 |
| 지원 분석 유형 | 6개 (EDA, 시계열, 세그먼트, 상관관계, A/B테스트, 예측) |
| AI 기능 | 자연어 코드 생성, 인사이트 자동 시각화 |
| 에이전트 수 | 5개 전문 분야별 AI 에이전트 |

---

## ✨ 핵심 기능

### 📊 자동 데이터 분석 파이프라인
```
CSV 업로드 → 자동 프로파일링 → EDA 대시보드 → AI 인사이트 → 예측 분석
```

| 기능 | 설명 | 핵심 기술 |
|:---|:---|:---|
| **AI 컬럼 분석** | 각 컬럼(Feature)의 의미와 분석 팁 자동 설명 | GPT-4, Rule-based Inference |
| **EDA 대시보드** | 데이터 품질 점수, 분포 히스토그램, 결측치/중복 분석 | Pandas Profiling, NumPy |
| **시계열 분석** | 트렌드 분석, 계절성 탐지, 이동평균선, 미래 예측 | Linear Regression, dateutil |
| **세그먼트 분석** | 그룹별 통계 비교, ANOVA 검정, 프로파일링 | SciPy Stats, ANOVA |
| **상관관계 분석** | 변수 간 상관계수 히트맵, 피어슨/스피어만 | Pandas Corr Matrix |
| **A/B 테스트** | 통계적 유의성 검정, 효과 크기, 신뢰구간 | T-test, Cohen's d |

### 🤖 AI 자동화 기능

| 기능 | 사용자 경험 | 기술 구현 |
|:---|:---|:---|
| **AI 인사이트** | 주요 패턴을 자동 발견하고 차트로 시각화 | GPT-4 + Recharts |
| **AI 전처리 진단** | 데이터 품질 이슈 자동 탐지 및 해결책 제안 | LLM Chain Prompting |
| **자연어 코드 생성** | `"결측치를 평균으로 채워줘"` → Pandas 코드 | Few-shot Prompting |
| **원클릭 실행** | 생성된 코드를 안전하게 바로 실행 | Sandboxed Execution |

### 🔮 예측 분석 엔진

| 기능 | 설명 |
|:---|:---|
| **시계열 예측** | 미래 값 예측 + 95% 신뢰구간 시각화 |
| **What-If 시뮬레이션** | "이 변수가 10% 증가하면?" → 연관 변수 영향 분석 |
| **이상 탐지** | IQR/Z-Score 기반 이상치 자동 탐지 및 심각도 분류 |

---

## 🏗 시스템 아키텍처

### 전체 구조
```
┌─────────────────────────────────────────────────────────────────┐
│                     FRONTEND (React 18 + Vite)                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  DataAnalysisPage   │  ChatPage   │  DashboardPage       │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  18개 분석 컴포넌트  │  7개 차트 컴포넌트  │  Skeleton UI │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │ REST API
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND (FastAPI)                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  /analysis/*  │  /agents/*  │  /chat  │  /ai-preprocess  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Multi-Agent Orchestrator (LangGraph)         │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │   │
│  │  │법률전문가│ │데이터분석│ │리스크   │ │전략가   │         │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  LLM Service (GPT-4 / Gemini)  │  Analysis Service       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### Multi-Agent 협업 패턴
```
User Query
    │
    ▼
┌───────────────────┐
│   Orchestrator    │  ← 쿼리 분석 및 에이전트 라우팅
└───────────────────┘
    │
    ├──► 법률 전문가 ──► 규정 검토
    ├──► 데이터 분석가 ──► 수치 분석
    ├──► 리스크 분석가 ──► 위험 평가
    └──► 전략가 ──► 종합 전략 수립
              │
              ▼
┌───────────────────┐
│ Response Merger   │  ← 다중 관점 통합
└───────────────────┘
              │
              ▼
       Final Decision
```

---

## 🛠 기술 스택

### Backend
| 기술 | 선택 이유 |
|:---|:---|
| **FastAPI** | 비동기 처리, 자동 API 문서화, 타입 검증 |
| **LangChain + LangGraph** | 복잡한 LLM 체이닝 및 Multi-Agent 오케스트레이션 |
| **Pandas + NumPy** | 대규모 데이터 처리 및 수치 연산 |
| **SciPy** | 통계 검정 (ANOVA, T-test, 상관분석) |
| **dateutil** | 다양한 날짜 형식 자동 파싱 |

### Frontend
| 기술 | 선택 이유 |
|:---|:---|
| **React 18** | 컴포넌트 기반 UI, 훅 기반 상태관리 |
| **Vite** | 빠른 HMR, 최적화된 빌드 |
| **Recharts** | React 친화적 데이터 시각화 |
| **TailwindCSS** | 유틸리티 기반 빠른 스타일링 |

### AI/ML
| 기술 | 활용 |
|:---|:---|
| **GPT-4** | 인사이트 생성, 코드 생성, 자연어 처리 |
| **Prompt Engineering** | 구조화된 JSON 출력, Few-shot 학습 |
| **ReAct Pattern** | Reasoning + Acting 에이전트 설계 |

---

## 🔬 핵심 구현

### 1. 자연어 → Pandas 코드 생성

```python
# 사용자 입력: "결측치를 평균으로 채워줘"

# System Prompt
"""
You are a Python data scientist. Generate clean Pandas code.
Rules:
1. Use 'df' as the DataFrame variable
2. Handle edge cases
3. Return JSON: {"code": "...", "explanation": "..."}
"""

# Generated Code
df['column'] = df['column'].fillna(df['column'].mean())
```

### 2. Multi-Agent 오케스트레이션

```python
class AgentOrchestrator:
    def __init__(self):
        self.agents = {
            "law_expert": LawExpertAgent(),
            "data_analyst": DataAnalystAgent(),
            "risk_analyst": RiskAnalystAgent(),
            "strategist": StrategistAgent()
        }
    
    async def process(self, query: str) -> Decision:
        # 병렬 에이전트 실행
        results = await asyncio.gather(*[
            agent.analyze(query) for agent in self.agents.values()
        ])
        # 결과 통합
        return self.merge_perspectives(results)
```

### 3. 자동 날짜 형변환

```python
# 다양한 날짜 형식 자동 인식
def auto_parse_date(column):
    # 1. pandas 기본 파싱
    result = pd.to_datetime(column, infer_datetime_format=True, errors='coerce')
    
    # 2. 실패 시 dateutil로 재시도
    if result.isna().sum() > len(column) * 0.5:
        result = column.apply(lambda x: dateutil.parser.parse(x, fuzzy=True))
    
    return result
```

---

## 📐 적용된 이론 및 기법

### 통계 분석
| 기법 | 적용 위치 | 설명 |
|:---|:---|:---|
| **ANOVA** | 세그먼트 분석 | 그룹 간 평균 차이 유의성 검정 |
| **T-test** | A/B 테스트 | 두 그룹 간 통계적 유의성 검정 |
| **Cohen's d** | A/B 테스트 | 효과 크기(Effect Size) 측정 |
| **IQR Method** | 이상 탐지 | 사분위수 기반 이상치 탐지 |
| **Z-Score** | 이상 탐지 | 표준편차 기반 이상치 탐지 |
| **Pearson/Spearman** | 상관분석 | 선형/순위 상관계수 |

### 시계열 분석
| 기법 | 적용 위치 | 설명 |
|:---|:---|:---|
| **Linear Regression** | 트렌드 분석 | 선형 추세선 및 예측 |
| **Moving Average** | 시계열 탭 | 3일/7일 이동평균선 |
| **CV (Coefficient of Variation)** | 변동성 분석 | 상대적 변동성 측정 |

### 소프트웨어 설계
| 패턴 | 적용 위치 | 설명 |
|:---|:---|:---|
| **Modular Architecture** | 전체 | 기능별 모듈 분리 (18개 분석 컴포넌트) |
| **Service Layer** | Backend | 비즈니스 로직과 API 분리 |
| **Repository Pattern** | Data Layer | 데이터 접근 추상화 |
| **Strategy Pattern** | LLM Service | 다중 LLM 제공자 지원 |

### AI/ML 기법
| 기법 | 적용 위치 | 설명 |
|:---|:---|:---|
| **ReAct Pattern** | 에이전트 | Reasoning과 Acting의 반복 |
| **Chain-of-Thought** | AI 인사이트 | 단계별 추론 유도 |
| **Few-shot Prompting** | 코드 생성 | 예시 기반 학습 |
| **Structured Output** | 전체 | JSON 스키마 기반 출력 |

---

## 📁 프로젝트 구조

```
multi_agent_decision/
├── backend/
│   ├── agents/                 # 🤖 Multi-Agent 시스템
│   │   ├── base.py            # 베이스 에이전트 추상 클래스
│   │   ├── registry.py        # 에이전트 레지스트리 (Factory)
│   │   ├── law_expert.py      # 법률 전문가 에이전트
│   │   ├── data_analyst.py    # 데이터 분석가 에이전트
│   │   └── risk_analyst.py    # 리스크 분석가 에이전트
│   │
│   ├── api/routes/analysis/   # 📊 분석 API (모듈화)
│   │   ├── core.py            # 파일 업로드, 프로파일링
│   │   ├── timeseries.py      # 시계열 분석
│   │   ├── segment.py         # 세그먼트 분석 (ANOVA)
│   │   ├── forecast.py        # 예측 분석
│   │   ├── ai_insights.py     # AI 인사이트 생성
│   │   └── code_generator.py  # 자연어 → 코드 변환
│   │
│   └── services/
│       └── llm_service.py     # LLM 추상화 (GPT-4 / Gemini)
│
└── frontend/src/
    ├── components/
    │   ├── analysis/          # 18개 분석 컴포넌트
    │   │   ├── EDATab.jsx
    │   │   ├── TimeSeriesTab.jsx
    │   │   ├── SegmentTab.jsx
    │   │   ├── ForecastTab.jsx
    │   │   └── AIPreprocessTab.jsx
    │   └── common/
    │       └── Skeleton.jsx   # 로딩 UI 컴포넌트
    │
    └── pages/
        └── DataAnalysisPage.jsx  # 메인 분석 페이지
```

---

## 🚀 실행 방법

### Quick Start

```bash
# 1. 프로젝트 클론
git clone https://github.com/yourusername/multi-agent-decision.git
cd multi-agent-decision

# 2. Backend 실행
cd backend
pip install -r requirements.txt
python -m uvicorn api.main:app --reload --port 8001

# 3. Frontend 실행 (새 터미널)
cd frontend
npm install
npm run dev
```

→ http://localhost:5173 에서 확인

---

## � 개발 로드맵

- [x] **Phase 1-8**: 기반 구축, Multi-Agent, 시각화, 분석
- [x] **Phase 9**: AI 자동화 (진단, 코드 생성, 자연어 실행)
- [x] **Phase 10**: 예측 분석 엔진 (시계열, What-If, 이상탐지)
- [ ] **Phase 11**: PDF 리포트 자동 생성
- [ ] **Phase 12**: 팀 협업 기능 (워크스페이스)
- [ ] **Phase 13**: 외부 데이터 연동 (Google Sheets)

---

## � 개발자

**AI-First 개발 방식**으로 설계부터 구현까지 진행

- 복잡한 비즈니스 로직의 모듈화 설계
- 확장 가능한 Multi-Agent 아키텍처 구현
- 사용자 경험 중심의 프론트엔드 설계

---

<div align="center">

**Built with ❤️ using AI-Driven Development**

*"복잡한 데이터 분석을 누구나 쉽게"*

</div>
#   D a t a _ a n a l y s i s  
 